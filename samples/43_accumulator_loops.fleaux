import Std;

// Python:
// def sum_items(items):
//     total = 0
//     index = 0
//     while index < len(items):
//         total += items[index]
//         index += 1
//     return total

let SumItems(items: Tuple(Int64...)): Int64 =
    ((0, 0),
     (state: Tuple(Int64, Int64)): Bool =
         ((state, 0) -> Std.ElementAt, (items) -> Std.Length) -> Std.LessThan,
     (state: Tuple(Int64, Int64)): Tuple(Int64, Int64) =
         (((state, 0) -> Std.ElementAt, 1) -> Std.Add,
          ((state, 1) -> Std.ElementAt, (items, (state, 0) -> Std.ElementAt) -> Std.ElementAt) -> Std.Add)
    ) -> Std.Loop -> (_, 1) -> Std.ElementAt;

((3, 1, 4, 1, 5)) -> SumItems -> Std.Println;

